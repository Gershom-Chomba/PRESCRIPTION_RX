@model IEnumerable<PRESCRIPTIONS_RX.Models.Drug>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formula)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Formula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TradeName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TradeName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TradeName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
